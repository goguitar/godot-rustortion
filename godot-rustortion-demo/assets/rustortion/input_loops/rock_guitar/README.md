# Rock Guitar Dataset (demo playback)

Rock guitar clips for the Godot Rustortion demo are stored here:

- `res://assets/rustortion/input_loops/rock_guitar/*.wav`

The demo scene (`res://main.tscn`) can switch input mode to `Non-stop play` and loop these files through the `GuitarPlayer` bus.
